PATCHES APLICADOS (Arepas-Burguer)
----------------------------------
- /app/admin/products/layout.tsx: agrega encabezado con botón "Crear producto" sin tocar tu page.tsx
- /app/admin/categories/CategoriesClient.tsx: interfaz para reordenar (↑/↓) y "Guardar orden" (POST /api/categories/reorder)
- /app/api/categories/reorder/route.ts: endpoint para persistir sortOrder
- /app/layout.tsx y /app/admin/layout.tsx: modo oscuro forzado por defecto
- /prisma/schema.prisma: ya existía sortOrder en Category

Observación:
- El header en productos se inyecta mediante layout anidado a nivel de /admin/products, evitando romper tu lógica actual.
- Asegúrate de que las páginas de categorías usen el componente CategoriesClient para ver los botones de reordenar.
- El build de Vercel ejecuta "prisma db push", por lo que, si se añadió "sortOrder", la BD se actualizará automáticamente.
